name: Go Tests

on:
  push:
    branches: [ "main" ]
    paths:
      - 'mi_guardian/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'mi_guardian/**'

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ./mi_guardian

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25'
        cache-dependency-path: mi_guardian/go.sum

    - name: Install dependencies
      run: go mod download

    - name: Run tests
      run: go test -v ./...
